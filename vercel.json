{
  "buildCommand": "cd backend && npm install && npm run build && cd .. && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/frontend/dist/index.html" }
  ]
}